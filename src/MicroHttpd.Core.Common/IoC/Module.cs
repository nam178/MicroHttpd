using Autofac;

namespace MicroHttpd.Core.Common.IoC
{
    public sealed class Module : global::Autofac.Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}
